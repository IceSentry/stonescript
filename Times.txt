var text_components = []

func add_text(panel, y)
  var text = ui.AddText()
  text.anchor = top_left
  text.dock = top_left
  text.h = 1
  text.x = 1
  text.y = y
  panel.Add(text)
  return text

var panel
?loc.begin | loc.loop
  // init
  panel = ui.AddPanel()
  panel.anchor = bottom_left
  panel.dock = bottom_left
  panel.x = 1
  panel.y = -1
  panel.h = 1 + 3 + 1

  // title
  var title_str = "Times"
  var title = ui.AddText(title_str)
  title.anchor = top_left
  title.dock = top_left
  title.w = string.Size(title_str)
  title.h = 1
  title.x = 1
  title.y = 0
  panel.Add(title)

  // text components
  for i = 0..2
    text_components.Add(add_text(panel, i + 1))

// update

func update_text(component, new_text)
  component.text = new_text
  var size = string.Size(new_text)
  component.w = size
  return size

var max_size
max_size = 0
for i = 0..text_components.Count() - 1
  var text = ""
  ?i = 0
    text = "↑ " + time.FormatCasual(loc.bestTime)
  ?i = 1
    text = "~ " + time.FormatCasual(loc.averageTime)
  ?i = 2
    text = "= " + time.FormatCasual(time)
  var size = update_text(text_components[i], text)
  max_size = math.Max(max_size, size)

panel.w = 1 + max_size + 1
